// Builds consistent output JSON with decision, clause refs

function formatResponse(decision, clauses) {
  // ...implementation will go here...
  return { decision, clauses };
}

module.exports = { formatResponse };
