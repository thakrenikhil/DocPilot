// Shared types (UserData, ClauseChunk, etc.)

// Example type definitions as JSDoc
/**
 * @typedef {Object} UserData
 * @property {number} age
 * @property {string} gender
 * @property {string} procedure
 */

/**
 * @typedef {Object} ClauseChunk
 * @property {string} id
 * @property {string} text
 */
